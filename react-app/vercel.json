{
  "devCommand": "vite --port $PORT",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/detail/:id", "destination": "/index.html" }
  ],
  "crons": [
    { "path": "/api/briefing-cron", "schedule": "30 22 * * *" }
  ]
}
