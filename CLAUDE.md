# ClaudeFinanceApp — 프로젝트 지침

## Git 브랜치 워크플로 (필수)

- **작업 브랜치는 `staging`** — 모든 코드 변경/커밋/푸시는 `staging`에서 한다.
- **`master`는 merge 시에만 체크아웃** — 배포용 브랜치. `git checkout master → git merge staging → git push` 목적으로만 잠시 전환하고, 직접 커밋하지 않는다.
- **세션 종료 시 `staging`으로 복귀** — 어떤 작업이든 끝난 뒤에는 `staging` 브랜치에 머무른 상태로 종료한다(merge를 위해 master로 갔더라도 마지막에 `git checkout staging`).

## 근거 기록 규약 (필수)

> **모델이 이미 알고 있던 값을 조회 결과가 반증하지 않는 것은 확인이 아니다.
> 확인은 원문에서 그 값을 추출했을 때만 성립한다.**

하드코딩된 외부 사실(일정·휴장일·상수 등)의 출처 주석은 **세 가지를 모두** 적는다.
셋을 채울 수 없으면 그건 확인이 아니므로 등급을 낮춰 적는다.

1. **확인 등급** — 아래 5단계 어휘 중 하나
2. **요청** — 실제로 던진 URL과 응답 상태. **실패도 그대로 남긴다**(403/504/파싱 실패 포함)
3. **발췌** — 원문에서 뽑은 문자열. 사람이 같은 URL을 열어 눈으로 대조할 수 있는 최소 단위

### 확인 등급 어휘

| 등급 | 뜻 |
|---|---|
| `자체실측` | **우리가 수집한 데이터로 값을 검증했다**(예: 거래일 캔들 유무). 제3자의 서술이 아니라 시장이 실제로 그렇게 움직였다는 증거 |
| `원문` | **발표 주체**의 문서·조문·API에서 직접 추출했다(연준·BLS·MSCI·한국은행·법제처·우주항공청·NYSE·해당 기업 IR) |
| `교차확인` | 발표 주체가 아닌 제3자, **독립 2경로 이상**이 일치 |
| `벤더확인` | **단일 데이터 벤더** 한 곳(Finnhub·네이버 등). 도달성은 좋지만 권위가 없고 반증할 두 번째 경로가 없다 |
| `관행추정` | **매년 개별 공고로 정해지는 값**을 과거 관행에서 추정. 관행이 일관되고 과거 사례가 실측돼도 해당 연도 공고 전까지 확정 아님. 반드시 확정 TODO(공고 예상 시점)를 함께 적는다 |
| `미확인` | 후보값. 규칙 도출·추정·검색 요약만 있는 상태 |

**`[원문]` 등급에는 문서 발행일을 필수로 기록한다.** 원문에도 asOf가 있으며, 발행 이후
제도가 바뀌면 원문이라도 낡는다. **발행일 이후 개정 여부 확인이 `[원문]` 등급의 성립 조건이다.**

**API는 문서가 아니라 응답이 사실이다.** 활용가이드·스펙 문서에도 발행일이 있으며
응답이 문서보다 앞설 수 있다. 스펙 문서의 서술을 근거로 "그 값은 안 온다"고 단정하지 말고
반드시 실호출로 확인한다.

> 실례(2026-07-29): KASI 특일정보 활용가이드 v1.4(2020-06-09)는 `getRestDeInfo`에 대해
> *"휴일 정보에 대한 조회로 제헌절은 해당 오퍼레이션에서 제공되지 않는다"* 고 기술했으나,
> 실호출은 `{"dateName":"제헌절","isHoliday":"Y","locdate":20260717}` 을 반환했다.
> 2026-04 개정이 응답에는 반영됐고 문서에는 반영되지 않은 상태였다.

> 실례: 2026년 월력요항(발행 2025-06-30)은 제헌절·노동절 개정(2026-04) **이전** 문서라
> 그 표에 제헌절이 없다. "원문에 없으니 휴장일이 아니다"라고 읽으면 정확히 거꾸로 간다.

- **`원문`과 `벤더확인`을 섞지 않는다.** 열람했다는 사실과 출처의 권위는 다른 축이다 —
  벤더 API가 HTTP 200을 줬다는 것은 "우리가 읽었다"는 뜻이지 "발표 주체가 그렇게 말했다"가 아니다.
- 여러 등급이 겹치면 `[자체실측 + 원문]`처럼 병기한다.

> **위 표는 서열이 아니라 축이다.** `자체실측`은 `원문`보다 상위 등급이 아니라
> **과거 항목 전용 검증 수단**이다. 미래 항목은 `원문` 등급이 유일한 방어선이며
> 자체실측으로 대체될 수 없다.
>
> **과거 항목 = 자체실측 + 원문 / 미래 항목 = 원문**

근거: 2026-07-29 휴장일 표 감사에서 누락 4건 중 자체실측이 잡은 것은 이미 지나간
`2026-07-17` **1건뿐**이고, 나머지 3건(`2027-05-03`·`07-17`·`07-19`)은 전부 조문이 잡았다.
실측 데이터가 아무리 깨끗해도 아직 오지 않은 날에 대해서는 침묵한다.

- 열람하지 못한 문서는 출처로 적지 않는다. 접근 실패는 "요청" 줄에 상태코드로 남긴다.
- 검색 결과의 **요약문·제목**은 원문이 아니다(`미확인`). 제3자 기사도 마찬가지다.
- 등급이 낮다는 것은 값이 틀렸다는 뜻이 아니라 **근거가 그 값을 지지할 만큼 강하지 않다**는 뜻이다.
  등급을 낮출 때 값은 건드리지 않는다 — 값 수정은 별도 판단이다.

배경: MSCI(2026-02)와 금통위(2026)에서 검색 요약본을 "원문 확인"으로 기록한 사고가 두 번
재발했다. 두 건 모두 값은 맞았고 **근거 기록이 틀렸다** — 그래서 값 대조가 아니라 기록 형식을
규약으로 고정한다. 적용례는 `react-app/api/_lib/macro-calendar.js` 상단 출처 블록.

`자체실측` 등급이 생긴 것도 같은 사고에서다. 휴장일 표의 제헌절 누락을 처음 드러낸 건 어떤
달력 사이트도 아니고 **우리가 이미 수집해 둔 일봉**이었다(2026-07-17에 3개 소스 전부 캔들 없음).
외부 사실을 하드코딩하기 전에 "우리 데이터로 이걸 검증할 수 있나"를 먼저 묻는다 — 단,
위 경고대로 그 답은 과거 구간에서만 나온다.
